import { GradesAveragePipe } from './grades-average.pipe';

describe('GradesAveragePipe', () => {
  it('create an instance', () => {
    const pipe = new GradesAveragePipe();
    expect(pipe).toBeTruthy();
  });
});
